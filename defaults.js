/**
 * Giá trị mặc định cài đặt (dùng chung popup + content script).
 */
var SEO_BASIC_DEFAULTS = {
  displayMode: "popup",
  panelPosition: "bottom-right",
  extensionEnabled: true,
  highlightNofollow: true,
  nofollowBgColor: "#ff5050",
  nofollowBgAlpha: 0.12
};
